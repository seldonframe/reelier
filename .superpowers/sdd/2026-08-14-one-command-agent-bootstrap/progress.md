# SDD ledger — plan: docs/superpowers/plans/2026-08-14-one-command-agent-bootstrap.md

## Exact base and ancestry

- Verified `codex/continuity-path-c-integration` at `b256a146cc31afe2613057ab92560a056c93f30e` before branch creation. Its tip is a report-only commit over independently repaired implementation commit `e379d8eb2d348e43f49c488fdb59bcfdf6070e27`.
- Created isolated worktree `C:/Users/maxim/CascadeProjects/reelier/.worktrees/reelier-init-up-implementation` on branch `codex/reelier-init-up-implementation` from exact base `b256a146cc31afe2613057ab92560a056c93f30e`.
- Planning ancestry verified: `e0118d003e70a2c0704abeac56661f7032451d21` has parent `2f7fcc21e785a102527ef6c43ab62f4f51a3c92b`; `1671b52c3e7cf8c741edd98b068dc0e2a833c493` has parent `e0118d003e70a2c0704abeac56661f7032451d21`; `2f7fcc2` is already an ancestor of base `b256a14` and is not the implementation base.
- Brought in accepted planning commits as `30d6e42` and `bea1010` by cherry-pick. Blob verification is exact: design `e013e68ad6867fe5576397d914a84975200a7071`; plan `a57330dba695ca1f654fe7093f3a1b0f740a088c`.
- Ruling: implementation base is exact continuity tip `b256a14`, not planning snapshot `2f7fcc2` — this preserves the latest independently repaired continuity state while retaining the accepted planning documents byte-for-byte — if wrong, implementation could omit later continuity evidence or be reviewed against the wrong ancestry.
- Ruling: cherry-pick the two accepted documentation commits onto the continuity tip without editing their file contents — their original parent is already in the continuity ancestry, so replaying only their document patches preserves both histories without treating the planning snapshot as the base — if wrong, commit identities differ from the accepted branch even though the governing document blobs remain identical.
- Incident: initial cherry-pick commands targeted the saved checkout after worktree creation. The saved checkout was restored immediately to its recorded commit and original untracked-file set; only the two introduced documentation files were removed. All subsequent commands use an explicit isolated-worktree path.

## Environment

- External installation is prohibited. The implementation worktree will reuse the existing dependency tree from the exact continuity worktree rather than run `npm install` or `npm ci`.
- Baseline attempt 1: `npm test` exited 1 during TypeScript compilation because the fresh worktree had no generated `dist` package exports for self-imports such as `reelier/authority/host`. No test body ran. Ruling: run the repository's local `npm run build` before repeating baseline tests — build output is required by the package self-import contract and does not install or contact anything — if wrong, a genuine source failure could be mistaken for setup state.
- Local build: `npm run build` exited 0 and generated the exact base package outputs without installation or network access.
- Baseline attempt 2: `npm test` completed with exit 1. The long Windows run exercised the suite but inherited Linux-host-only failures in Authority runtime/dispatch/HTTP/receipt tests; it also exposed a fresh-worktree Eve fixture dependency path, repaired by a local junction to the exact base fixture dependencies, and an existing declaration allowlist mismatch in `test/authority/package.test.ts`. This is consistent with the accepted branch's recorded Windows/Linux limitation and is not attributed to bootstrap code, which does not yet exist.
- Ruling: continue with each task's focused gates on this accepted Windows base, and repeat the plan's one required full-suite attempt at Task 8 with exact failures/skips retained — the accepted plan explicitly says native Windows gates may prevent green and forbids translating them into passes — if wrong, an inherited base regression could be carried into the final branch and must remain visible in the final evidence.

## Constitutional and ownership inputs read

- Read the accepted bootstrap design and implementation plan completely.
- Read `docs/superpowers/specs/2026-08-14-continuity-adapter-conformance-design.md`, current branch `AGENTS.md`, Cloud `FOUNDATION.md`, and the engineering-system `BUILDING-COMPASS.md` completely.
- Verified and read the managed-Cell complement at immutable Cloud commit `892a91d802391e233c72c527fee60951862eee71` (`docs: plan managed Authority Cell productization`).
- Ruling: OSS owns `init`/`up`, profile governance, governed host composition, route discovery, supervision, remote Cell client/session contracts, and A/B/C conformance; Cloud owns entitlement, managed provisioning, isolated deployment/secret custody, founder UI, pricing, and lifecycle — this is the exact split frozen at Cloud `892a91d`; if wrong, this branch could absorb control-plane or custody responsibilities and create two kernels.
- Ruling: managed Cell is the founder default, while `init`/`up` only connect to an already admitted remote Cell and never provision or launch Linux — advanced self-hosting is an already-provisioned Linux endpoint under the same protocol — if wrong, founder onboarding could falsely claim local Cell ownership or weaken `assertLinuxAuthorityCellHost`.

## Exhaustive task/interface preflight

| Pair | Producer → consumer / shared file | Finding and ruling |
|---|---|---|
| 1→2 | Profile artifacts, offline verifier, contract digest; shared `test/authority/package.test.ts` | Compatible. Task 2 may consume inert public artifacts but host admission remains package-internal and opaque. |
| 1→3 | `OUTCOME_PROFILE_CONTRACT_V1_DIGEST`; shared `package.json` | Compatible. Task 3 must consume Task 1's generated digest, not add profile members to Authority Contract v1. |
| 1→4 | Profile/pack classification informs route outcome lane | Compatible. Discovery can report capability only; it cannot activate or certify. |
| 1→5 | Draft builder, governance status, exact profile/trust digests | Conflict found: Task 5 says “externally configured profile roots,” while the spec and Task 2 require fixed operator-owned trust-directory resolution. Ruling below. |
| 1→6 | Profile/activation/trust digests in remote binding | Compatible only as exact digest joins; the supervisor cannot mint or admit governance. |
| 1→7 | Public governance/verifier contract; shared `package.json` | Compatible. Packed checker uses public offline verification and public governed factory only. |
| 1→8 | Profile claims and contract digest feed docs/baseline | Compatible. Documentation must preserve non-authorizing offline-verification language. |
| 2→3 | Host admission snapshot supplies profile/activation/trust identities used by session-binding schema | Compatible. Task 3 freezes only sanitized public binding data, never the handle. |
| 2→4 | Admitted/profile status informs activated/enforced route fields | Compatible. Discovery receives sanitized classifications/evidence only. |
| 2→5 | Read-only governance status helper and fixed trust directory | Compatible under the Task 5 ruling; bootstrap never receives `AdmittedProfileGovernanceV1`. |
| 2→6 | Shared `src/authority/host/governed-cell.ts`; server binding provider; host factory | Conflict found: Task 6 interface shorthand mentions `AdmittedProfileGovernanceV1 | null`, but `up` must not receive the handle. Ruling below confines it to the factory internals. |
| 2→7 | Governed Cell public factory and governed receipt wrapper | Compatible. Packed consumer must prove private loader/deep paths remain absent. |
| 2→8 | Host-only admission and receipt claims feed guide/non-claims | Compatible. Docs must distinguish public verification from host admission. |
| 3→4 | `RouteCoverageV1`, route normalization, bootstrap contract | Compatible. Task 4 implements collectors over Task 3's closed rows. |
| 3→5 | `AgentProjectV1`, report/runtime/route records and build identity | Compatible. Named init writes Task 3 records under separate bootstrap state. |
| 3→6 | `AgentProjectV1`, `RuntimeDescriptorV1`, session binding, supervisor status | Compatible. Task 6 validates all pins before reading environment or spawning. |
| 3→7 | Public bootstrap package and contract; shared `package.json` | Compatible. Task 7 consumes the packed public surface. |
| 3→8 | Contract/build identities feed release baseline | Compatible. Evidence must state missing tarball integrity as null/absent, not fabricate it. |
| 4→5 | Route discovery output becomes named-init route artifact | Compatible. Legacy `.reelier/init/` bytes/checkpoints remain untouched. |
| 4→6 | Fresh rediscovery and route-row aggregates | Compatible. `up` must downgrade stale/unreadable evidence to unknown. |
| 4→7 | A/B/C bypass and completeness rows | Compatible. Checker must keep equivalent routes as separate evidence rows. |
| 4→8 | Founder coverage copy | Compatible. All aggregates derive from rows and completeness remains not-proved. |
| 5→6 | Shared `src/cli.ts` and `test/authority/authority-cell-connection.test.ts`; printed pinned command and project consumed by `up` | Compatible if Task 6 preserves Task 5 parsing/help and never changes init semantics. |
| 5→7 | Real named-init transaction consumed by packed journey | Compatible. Task 5 constructs/persists command; Task 7 alone executes the packed clean command. |
| 5→8 | Onboarding commands and rollback semantics | Compatible. Guide must use the exact pinned command printed by init. |
| 6→7 | Supervisor, remote requester/status, session binding and restart semantics | Compatible. Task 7 must exercise public/runtime seams without private admission access. |
| 6→8 | Runtime status and founder output language | Compatible. External runtime is never rendered locally owned; Path labels stay verbose-only. |
| 7→8 | Conformance/packed outputs and exact evidence counts | Compatible. Task 8 consumes immutable Task 7 evidence, not inferred success. |

### Per-task internal consistency

| Task | Tests ↔ implementation ↔ allowlist check | Finding / ruling |
|---|---|---|
| 1 | RED tests name missing profile governance; GREEN schemas/builder/runtime/export/package files are all listed. | Internally consistent. RED commit contains only listed tests/fixtures; GREEN owns generated outputs. |
| 2 | RED no-dispatch matrix; GREEN loader/admission/factory/receipt and regressions all listed. | Internally consistent. Public factory may export through existing `./authority/host`; loader/brand stay out of barrel/package allowlists. |
| 3 | RED module/schema inertness; GREEN bootstrap/routes/runtime records, build identity and package export all listed. | Internally consistent. Installed-build digest must exclude its own digest record and reject symlinks/case collisions. |
| 4 | RED bypass/freshness rows; GREEN adapters/discovery plus existing coverage/initialization translations all listed. | Internally consistent. No `INIT_CHECKPOINT_IDS`, report-byte, or existing artifact change is allowed. |
| 5 | RED legacy compatibility, maker/checker, crash and path tests; GREEN bootstrap transaction/init/wrap/CLI all listed. | Governance-root wording conflict resolved below. Exact-version wrapping stays argv-based, never a shell string. |
| 6 | RED no-spawn/session mutation matrix; GREEN runtime registry/supervisor/client/server/CLI all listed. | Admission-handle shorthand conflict resolved below. Managed/self-hosted Cells are never launched by `up`. |
| 7 | RED public black-box checker and packed journey; GREEN conformance/checker/package script all listed. | Internally consistent. Local tarball installation is permitted only inside the plan's disposable offline consumer; no registry/network/global install. |
| 8 | RED doc falsifiers; GREEN guide/baseline/README/SPEC/AGENTS and private report listed. | Conflict found: repository test pins `AGENTS.md` and `CLAUDE.md` byte identity, but `CLAUDE.md` is omitted. Ruling below adds only that mirror path to Task 8's effective allowlist. |

## Preflight rulings

- Ruling: Task 5 governance import uses only Task 2's read-only sanitized status path over `<homedir>/.reelier/trust/outcome-profiles/...`; caller/project-supplied roots can support non-authorizing offline inspection only and never bootstrap activation — the spec is binding over the ambiguous phrase “externally configured profile roots” — if wrong, init could become its own trust-root selector.
- Ruling: Task 6's supervisor and `up` never consume `AdmittedProfileGovernanceV1`; only `createGovernedAuthorityCell` retains the handle internally long enough to derive a detached admission snapshot for the authenticated binding endpoint — if wrong, the remote client could cast public verification into host authority.
- Ruling: Task 8's effective tracked allowlist includes `CLAUDE.md` solely as the byte-identical mirror of `AGENTS.md`, because the accepted base has an executable invariant test requiring exact equality — no other allowlist expansion is authorized — if wrong, either the documented capability surface drifts between agent instruction files or the existing mandatory test fails by construction.
- Ruling: per-task review uses one fresh independent reviewer that emits separate spec-compliance and code-quality verdicts, matching the SDD review template and the user's singular “independent task reviewer” instruction; maker remains different from verifier — if wrong, the plan's phrase “first ... then” may have intended two reviewer agents rather than two ordered verdicts, costing an extra review pass.
- Ruling: Task 7 may install only the locally created exact tarball into a disposable offline consumer as an explicit hermetic acceptance step; it may not install from a registry, globally, into the saved checkout, or outside disposable test roots — if wrong, the user's “no external installation” prohibition may require replacing that packed acceptance with a non-installing proof, weakening the accepted clean-consumer test.

## Task execution

- Task 1: BASE `bea1010443e1308053fbff87c9010962468190bb`; brief `task-1-brief.md`; implementer `/root/task1_profile_governance`; exhaustive tracked allowlist is the Task 1 `Files:` block; strict RED subject and GREEN subject dispatched verbatim.
- Task 1 RED: `5dd732d647a94dea44456f127d78d9049a7da119` (`test(authority): specify outcome profile governance`), parent exact BASE `bea1010`; commit contains only the three allowed test paths. Implementer reports the compile failed on the two intended missing profile modules; full command/output will be checked from the task report before review.
- Task 1 GREEN candidate: `766fdbb668431733acc90b6ffc0b67d3179758f6` (`feat(authority): separate profile certification and activation`); report records 23/23 focused tests, both contract checks, build, and diff check passing; branch clean and allowlist exact.
- Task 1 review 1: spec ❌, quality Needs fixes. Open: uncompilable governed-receipt schema refs; replay digest mutation tests short-circuit; Date intrinsic-time hardening; Task 2-blocking host-API absence assertion; unrelated declaration allowlist correction needs removal or explicit inherited-baseline evidence. Full findings: `task-1-review.md`.
- Task 1 fix round 1 RED: `5678ea39a18500f8c3f15d8dcca71ebb2ecaa50f` (`test(authority): expose outcome profile review gaps`), parent `766fdbb`; only the two allowed Task 1 test files changed. Production fixes began only after this commit.
- Task 1 fix round 1 GREEN: `6ed54280ff579a1e035cff5fbb699af481e71954` (`fix(authority): close outcome profile review gaps`), parent `5678ea3`; scoped package `review-766fdbb..6ed5428.diff`.
- Task 1 fix round 1/5: 5 addressed, 0 open. The same independent reviewer confirmed the schema graph compiles under its custom IDs, replay mutation assertions reach verification, intrinsic time handling resists hostile `Date` methods, the package test no longer blocks Task 2 host APIs, and the inherited declaration correction is documented. No new breakage was found.
- Task 1 fresh root verification: TypeScript test compilation exited 0; 25/25 focused tests passed; `check:authority-contract`, `check:outcome-profile-contract`, `git diff --check`, and clean tracked status all exited 0. Final outcome-profile contract digest: `sha256:80f3a2a799ac84a0ad11a6ef0b363b2c54ea7ce13c70c508d6e998c455b26a5e`.
- Task 1: complete (commits `bea1010..6ed5428`, review clean).
- Task 2: BASE `6ed54280ff579a1e035cff5fbb699af481e71954`; brief `task-2-brief.md`; implementer `/root/task2_profile_admission`; exhaustive tracked allowlist is the Task 2 `Files:` block; strict RED subject and GREEN subject dispatched verbatim.
- Task 2 RED: `bce46f07cfe82754eff68e99cfa6b786201f30e9` (`test(authority): specify profile admission`), parent exact BASE `6ed5428`; commit contains only the seven allowed Task 2 test paths. TypeScript compilation failed only on the intended missing Task 2 modules/internal admitted constructor; production work began after the RED commit.
- Task 2 GREEN candidate: `cc529e5e4136eb9e58cd5f39816798f9636bb715` (`feat(authority): admit independently governed profiles`); report records 64/64 focused/kernel tests, TypeScript, build, both contract checks, and diff check passing; branch clean and 13-file allowlist exact. Independent review package: `review-6ed5428..cc529e5.diff`.
- Task 2 review 1: spec FAIL, quality FAIL, `CHANGES REQUIRED`. Blocking findings: admitted governance is self-compared and can start an unrelated configured definition; independently valid governance and Authority receipt bundles can be paired; the required no-dispatch matrix is inert; the operator root identity is not pinned across the six-file cold read; and required Linux-child/mutation evidence is absent. Full findings: `task-2-review.md`.
- SDD STOP CONDITION â€” accepted-plan/interface contradiction: `SignedTenantProfileActivationV1` requires independent joins for Job Card, contract, deployment, route authority, and authority trust head, but the frozen `ProfileGovernedAuthorityReceiptV1` contains only the four profile-side digests plus opaque `innerReceiptDigest`, and `AuthorityReceiptBundle` independently exposes only the contract of those five authority bindings. Job Card, deployment, route-authority, and authority trust-head proofs are absent. Completing the verifier requires an architecture choice among expanding the outer contract with signed authority proofs, widening verifier inputs with externally anchored proofs, or defining a governed inner receipt; each changes Task 1 contract/schema/generated digest paths outside Task 2's exhaustive allowlist and amends the accepted plan. Ruling: stop before a fix wave rather than invent false digest equivalences or silently widen the accepted interface â€” this is one of the four authorized SDD stop conditions â€” if wrong, continuing could either violate the frozen plan/allowlists or emit a receipt that falsely attributes profile governance to an unrelated effect.
- Task 2 remains incomplete at candidate `cc529e5`; no review fix commit was started. Tasks 3â€“8 and whole-branch review were not begun.
- User architecture ruling: approved amending the plan and outer governed-receipt contract to carry independently verifiable, purpose-bound signed Authority evidence while leaving `AuthorityReceiptBundle` and Authority Contract v1 unchanged. Execution resumes only after a docs-only amendment commit and independent amendment review.
- Ruling: preserve the existing activation `trustHeadDigest` as the profile-governance head and add a distinct `authorityTrustHeadDigest`; the two trust chains have different keys, purposes, event preimages, and head algorithms and must never be equated â€” if wrong, maker/verifier role separation collapses into an impossible shared digest.
- Ruling: activation binds a stable closed route-scope digest, not the per-request `RouteAuthoritySnapshotV1` digest; the exact dynamic snapshot is joined only at receipt time by a Cell-signed outer binding â€” if wrong, activation would need to predict request materialization, slot instance/version, identity, generation, and expiry before the request exists.
- Ruling: the new outer binding uses existing `authority-evidence` crypto purpose and an active distinct Authority Cell evidence signer; local-gate, topology, lease, identity, profile operator/certifier, and Job Card signer keys are not substitutes â€” if wrong, unrelated trust purposes could mint governed evidence.
- Task 2 approved amendment candidate: `5c7d0288097c7b7557f432d9bd6c6d1768b40f65` (`docs: amend governed authority evidence`), parent `cc529e5`; exactly the design and plan documents changed. The amended Task 2 names a 28-path fix-wave allowlist, explicit forbidden Authority ABI paths, exact RED/GREEN subjects, and all new evidence interfaces. Review package: `review-cc529e5..5c7d028-amendment.diff`.
- Task 2 amendment review 1: architecture/spec FIX FIRST; executability/quality FIX FIRST; `CHANGES REQUIRED` with 3 Critical, 3 Important, and 2 Minor findings. Full review: `task-2-amendment-review.md`.
- SDD STOP CONDITION â€” amended plan still lacks three authoritative producers: (1) normal host publication creates `LocalAuthorityPublication`, not a complete `AuthorityReceiptBundle`; the only complete producer is certification-only `src/authority/certification/lifecycle-receipts.ts` with purpose-specific key custody; (2) `loadAuthorityDeployment` parses but does not verify/select eligible contract candidates, while the sole eligibility algorithm is private inside `src/authority/gate.ts`; (3) current stable route/config/pack inputs cannot derive `projectionSchemaDigest`, and `RouteAuthoritySnapshotV1` lacks tenant/definition context, so `AuthorityRouteScopeV1` cannot be produced at activation time as written. Ruling: do not begin RED until ownership is chosen for a general host receipt-authority producer, one shared gate candidate selector, and a stable route projection commitment â€” if wrong, implementation would add a second receipt kernel/verifier or trust a caller-supplied digest.
- Environment stop: an actual Linux kernel is reachable only through the existing `docker-desktop` WSL distribution, which has no Linux Node runtime; the Docker daemon is unavailable and external installation/startup is prohibited. The amended non-skipping Linux child gate therefore has no authorized executor on this host. Ruling: require an explicit already-available executor or approve an honest platform-gated local skip with Linux CI/Task 7 evidence remaining mandatory â€” a platform mock or Windows Node under WSL is not Linux evidence.
- Additional required amendment fixes: revalidate profile/Authority trust immediately before gate/reservation and bind that observation time; range-scope whitespace checks over `cc529e5..HEAD`; freeze deployment aggregate digest preimages; repeat the complete governed options interface without placeholders.
- Task 2 remains incomplete at docs amendment `5c7d028`; no RED fix-wave commit or implementation changes were started after the review stop.
- User second architecture ruling: approved extracting a general purpose-separated host receipt-authority producer, sharing one gate-owned eligible-candidate selector with governed preflight, adding a route-owned stable projection-schema commitment, revalidating profile/Authority trust immediately before reservation, and treating the unavailable local Linux Node execution as an explicit platform gate while retaining mandatory Linux evidence in later CI/Task 7. A second docs-only amendment and independent review are required before RED.
- Task 2 second amendment candidate: `481561a19641b722d6a48f7f41b512b56c26cad6` (`docs: complete governed authority execution seams`), parent `5c7d028`; exactly the design and plan changed. It expands the fix-wave ceiling to 43 paths, defines the general six-signer bundle constructor, shared gate selector, route-owned projection commitment, pre-invoke/pre-reserve trust revalidation, immutable governed recovery, fixed digest algorithms, and honest Linux platform gate. Review package: `review-5c7d028..481561a-amendment-2.diff`.
- Task 2 second amendment review: spec/executability `FIX FIRST`, `NOT APPROVED`; four Critical and one Important finding. Full review: `task-2-amendment-2-review.md`.
- SDD STOP CONDITION — the second amendment does not reach the canonical certified prepared-dispatch path: `src/authority/host/dispatch.ts` never invokes publication on that branch, yet the plan forbids that source and its focused coordinator test. It also omits the existing synthetic `reservation` receipt phase, incorrectly treats four artifact-purpose subkeys as ordinary `AuthorityKeyDescriptorV1` keys instead of using the existing signed `CertificationArtifactKeyBindingV1` and human commitment, and leaves no durable pre-effect governed foundations for post-send crash/sign/storage recovery. Ruling: do not begin RED until the architecture explicitly authorizes an optional pre-send governed reservation publication after prepared CAS and before provider send, chains terminal/reconciliation publication to that root, preserves the existing artifact-key authorization model without widening Authority Contract v1, and freezes `DispatchPublication` compatibility to the inner receipt-value digest and inner evidence digest — if wrong, the canonical governed path could either emit no receipt, break prior chaining, accept unauthorized signing keys, or lose reconstructible evidence after an effect.
- Task 2 remains incomplete at docs amendment `481561a`; no third amendment or RED fix-wave commit has begun. A third explicit architecture approval is required because this changes the newly approved dispatch/publication and signer-authorization seams and expands the tracked allowlist beyond 43 paths.
- User third architecture ruling: approved adding the canonical certified prepared-dispatch publication seam and focused coordinator test; a durable governed reservation root after prepared CAS and before provider send; terminal/reconciliation chaining and restart verification without resend; the existing signed `CertificationArtifactKeyBindingV1` plus human commitment for the four artifact subkeys; the omitted `reservation` constructor phase; and inner-compatible `DispatchPublication` return values. Authority Contract v1, `AuthorityReceiptBundle`, its verifier, and the ledger remain frozen. A third docs-only amendment and fresh independent review are required before RED.
- Task 2 third amendment candidate: `f10bc5186298d1501d8e73d91b216ee4e38db500` (`docs: complete governed dispatch publication`), parent `481561a`; exactly the design and plan changed. The executable ceiling is 47 unique paths: the prior 43 plus certified dispatch source/test and lifecycle artifact-key-binding source/test. It specifies the post-CAS/pre-send governed reservation publication, exact reservation phase, durable restart chain, existing signed artifact-key authorization, and inner-compatible publication return semantics. Mechanical doc review and range whitespace checks pass; tracked status is clean. Independent amendment review is in progress.
- Task 2 third amendment review: `FIX FIRST`; two Critical architecture findings and no non-blocking findings. Full review: `task-2-amendment-3-review.md`. The 47-path equality, dispatch reachability, reservation bytes, pre-send refusal, artifact-binding model in principle, inner-compatible references, and frozen-file gates are otherwise sound.
- SDD STOP CONDITION — the third amendment still lacks (1) a read-only publisher→coordinator recovery handshake for a terminal governed node durably stored before the ledger terminal transition, so restart would otherwise fork, fail forever, or create a second lifecycle owner; and (2) an exact externally anchored artifact-key authorization handoff into the constructor plus an executable offline order. Ruling: add an optional closed `loadDurableHead`/equivalent seam whose strictly verified phase/terminal kind/inner receipt/evidence references let the coordinator finish the matching ledger transition without resend or sibling publication; and add a package-private validated-signing-authority factory/brand consuming signed readiness, current descriptors/events, expected Cell/task/time, binding+commitment, and exact signer capabilities. Offline verification must parse the outer envelope/binding first for candidate roots, run the unchanged inner verifier, then prove the binding against external anchors and match exact inner signer IDs/SPKIs/purposes; receipt-carried roots alone never authorize themselves. Both corrections fit the existing 47 paths and do not justify changing Authority Contract v1, the inner bundle/verifier, or ledger wire, but they change the authoritative trust/recovery contract and require explicit approval plus another docs-only review before RED.
- User fourth architecture ruling: approved the optional strictly verified durable-head recovery seam and the package-private externally validated signing-authority brand/factory, including the corrected non-self-anchoring offline verification order, within the same 47-path ceiling. A fourth docs-only amendment and fresh independent review are required before RED.
- Task 2 fourth amendment candidate: `c091e525408d2b605ea2894d2ad8616c83072a8c` (`docs: close governed recovery and trust handoff`), parent `f10bc51`; exactly the design and plan changed. It freezes the closed durable-head adoption/root-only ambiguity contract, the private validated-signing-authority WeakMap handoff using the full external Job Card trust pin/current events, and the outer-first/provisional-root/externally-authorized offline verification order. Cumulative stale callback/direct-descriptor/inner-first wording was removed. Allowlist remains 47/47 unique; RED stages 22 allowed tests; GREEN names all 47; tracked status and range checks are clean. Independent amendment review is in progress.
- Task 2 fourth amendment review: `FIX FIRST`; three Critical architecture findings, one Important interface finding, and no non-blocking findings. Full review: `task-2-amendment-4-review.md`. Durable-head structure, storage order, non-self-anchoring offline intent, 47/22/47 scope discipline, and frozen ABI/schema/ledger gates are otherwise sound.
- SDD STOP CONDITION — amendment 4 still (1) rejects its own normal steady-state ambiguous head on restart; (2) requires a full Job Card trust pin in the sole validator while the frozen certification lifecycle material/call site cannot supply or register it; (3) claims profile-certifier/operator segregation without passing those independently verified profile-domain keys; and (4) types `loadDurableHead` with a state projection missing tenant/request/capability identities required by its checks. Ruling: add the exact ambiguous steady-state no-op mapping; register a private validated lifecycle context at the existing certification Cell call site where the full pin exists, which requires authorizing `src/authority/certification/cell.ts` and `test/authority/certification-cell.test.ts`; add an exact private segregation-anchor/provenance handle with profile certifier/operator and every applicable forbidden-role signer ID/SPKI; define lifecycle-only inapplicable anchors honestly; and widen the already owned dispatch state `Pick` or use a dedicated closed recovery query. This changes the recovery/trust contract and expands the ceiling from 47 to 49 paths, so explicit approval and another docs-only review are required before RED.
- User fifth architecture ruling: approved the 49-path ceiling and explicitly required a first-principles reset. Ruling: stop locally patching amendment text; independently derive the complete dispatch/recovery state machine and the complete trust/evidence authority graph from existing runtime invariants and actual call-site inputs, then synthesize one cumulative replacement amendment and review it as a whole — if wrong, another incremental correction could preserve a hidden contradiction between independently reasonable seams.
- First-principles dispatch/recovery ruling: keep the concrete ledger frozen and keep `ambiguous` rows result-less, because `FsAuthorityLedger` rejects an ambiguous transition carrying `resultDigest`. The coordinator remains the only ledger lifecycle owner; the immutable governed receipt store remains the only receipt-chain/prior owner. Reconciliation loads the exact verified ambiguous inner prior from the store. A matching normal/recovery ambiguous head is a restart no-op; a durable reconciliation head is adopted. Post-CAS/no-root recovery refuses, while pre-CAS reserved recovery preserves existing behavior. Store append is semantic compare-and-set/idempotent and enforces one root/one successor per prior so concurrent recoverers cannot fork.
- First-principles trust ruling: derive historical authority at the signed `observedAt` from external profile roots and the full external Job Card trust pin/current-event prefix; report verifier-time revocation/expiry separately rather than invalidating historical evidence. Authorize the four artifact subkeys through the externally verified binding+human commitment before running the unchanged inner verifier. Use the same active direct `authority-evidence` key for inner evidence and the domain-separated outer binding, because current readiness semantics permit only one direct key per Cell purpose; maker/verifier separation does not require two keys. The certification Cell registers the full validation context at the only call site that holds it. Governed admission privately retains profile certifier/operator ID+SPKI segregation anchors.
- First-principles gate-key ruling: governed composition must use a package-private read-only `loadExistingLocalGateSigner` that never creates directories or keys and pass that exact pinned signer into the internal runtime core. Implementing this only in `local.ts` would duplicate Ed25519 parsing and retain a path-reopen race. Therefore add `src/authority/host/gate-signer.ts` and `test/authority/gate-signer.test.ts`, expanding the coherent ceiling from 49 to 51 paths; public legacy `loadOrCreateLocalGateSigner` behavior and host barrels remain unchanged.
- Task 2 first-principles amendment candidate: `413e3c824f1cc15e886627b35908ea50ab776d94` (`docs: rederive governed authority from first principles`), parent `c091e52`; exactly the design and plan changed. It replaces the cumulative contradictory Task 2 architecture with the authoritative recovery state table and trust graph, a result-less ambiguous ledger/store-sourced prior design, semantic CAS receipt storage, full-pin Cell handoff, as-of historical trust semantics, one domain-separated evidence key, opaque mode-specific trust/signing handles, retained profile segregation anchors, and read-only pinned gate-key loading. Effective ceiling is 51 unique paths; RED stages 24 tests/fixtures; GREEN names all 51; tracked status and range checks are clean. Fresh independent first-principles review is required before RED.
- Task 2 first-principles review: architecture coherent in substance, verdict `FIX FIRST` for three bounded interface/state-table gaps within the existing 51 paths. Full review: `task-2-first-principles-review.md`. Required fixes: permanently refuse the real markerless `dispatched` partial-CAS crash state before durable query/publisher activity; give reservation publication an exact closed identity input and domain-separated full-intent digest preimage; and require an exact enumerable `TrustRootEntry[]` direct-root option so the outer verifier can externally authorize and combine the four artifact subkeys without opaque/self-carried roots. No filesystem-ledger, Authority Contract, inner verifier, package, config, or new path change is required. A bounded docs fix and scoped re-review are authorized under the existing review loop; RED remains blocked until clean.
- Task 2 first-principles bounded fix candidate: `c1dbd1f7036aac895e6eca9df4d8c303d31b26c5` (`docs: close first-principles review gaps`), parent `413e3c8`; exactly the design and plan changed. It adds the markerless partial-CAS permanent refusal, shared typed reservation identity/full stored-intent digest, exact enumerable direct-root verification options, and stale-history cleanup. The 51/24/51 mechanics, frozen paths, range checks, and tracked cleanliness remain intact. Scoped same-reviewer re-review is required before RED.
- Task 2 first-principles scoped re-review: markerless refusal and typed reservation identity are closed; one Important plan-only trust-root gap remained because Job Card descriptors provide no `principalId` mapping. Ruling: do not infer principal identity; authenticate the deployment snapshot through the verified outer signature, validate the exact five direct roots by independently available tenant/signer/SPKI/purpose/as-of activity, join their recomputed `trustRootSetDigest` to the authenticated deployment snapshot, then append the four externally authorized artifact roots for unchanged inner verification.
- Task 2 direct-root bounded fix candidate: `1d837a96f3221991f5026fb14a7e20d1aa92e0dd` (`docs: bind governed direct root set`), parent `c1dbd1f`; exactly the design and plan changed. The nonexistent principal inference is removed and the authenticated five-root-set digest join/order is frozen. Mechanics remain 51/24/51, range checks clean, tracked status clean. Final scoped same-reviewer verdict is pending before RED.
- Task 2 first-principles final scoped re-review: `APPROVED`; no blockers. The same reviewer confirmed all three bounded findings are closed, the five-direct-root digest join is executable and non-self-anchoring, mutation attribution is honest, the complete 51/24/51 scope is consistent, and all frozen boundaries remain intact. Task 2 fix-wave RED may begin from exact base `1d837a96f3221991f5026fb14a7e20d1aa92e0dd`.
- Task 2 first-principles RED: `f97f9cfe9c181e532f05d529fc692fddaf42dc76` (`test(authority): expose governed admission review gaps`), parent exact approved base `1d837a9`; exactly 24 approved test/fixture paths changed. TypeScript fails only on intended missing Task 2 surfaces; coordinator focused run has 8 existing passes and one intended markerless-dispatch failure; broader focused execution reaches only intended missing module/export/contract gaps. Shared fixture registers no tests, the non-Linux child is honestly skipped with the approved reason, `git show --check` and tracked status are clean. Production GREEN is authorized.
- Task 2 first-principles GREEN candidate: `f935332b850857045002f238a69667c3d442b149` (`fix(authority): bind governed profiles to verified authority`), parent RED `f97f9cf`. RED+GREEN range changes 49 of 51 authorized paths and zero unexpected paths; GREEN commit itself changes 29 production/generated/test paths. Clean-tree evidence: TypeScript, build, Authority Contract check, eight-member Outcome Profile contract check, frozen-path/range/whitespace/immutable-subject checks pass; adapter digest remains `sha256:7f46242b26d9c921f4e1ec9de6418ac5fc8c03d70c4415c25e799ae0e73a1512`; exact focused suite is 220 total / 216 pass / 0 fail / 4 documented skips. Tracked status clean. Full report: `task-2-report.md`. Fresh independent task review is required; Linux positive factory evidence remains explicitly owed by Task 7/CI.
- Task 2 implementation review 1: spec FAIL, code quality/executability FAIL, `CHANGES REQUIRED`; full findings `task-2-final-review.md`. The reviewer reproduced bounded implementation defects within the approved 51-path surface: production reservation publication receives only recovered state and lacks accepted signed provenance; governed admission self-feeds Job Card/route/Authority-head activation values; lifecycle trust handoff is only a WeakSet mark; artifact binding is not joined to the actual current evidence signer; fresh revalidation is discarded; multiple RED matrices are inert/import-only; semantic-CAS equal races throw and store paths lack physical confinement; operator-root digest omits physical identity; and public signer/kind surfaces deviate. Architecture remains approved and no frozen path expansion is needed. A strict tests-first bounded review-fix wave plus scoped same-reviewer re-review is authorized.
- Task 2 implementation review-fix RED: `b4e9140f8a49313527f6d327170e1c6ea4d58320` (`test(authority): expose governed implementation gaps`), parent GREEN candidate `f935332`; exactly eight allowlisted test/fixture paths and no production changed. TypeScript fails only because the extra public `authorityBindingSigner` remains and `ProducedReceiptKindV1` has the wrong union. Focused real-boundary suite: 69 total / 63 pass / 5 intentional failures / 1 exact approved Linux skip. The five failures reproduce accepted-state provenance loss before reservation publication, stale lifecycle authority after fresh-pin revocation, alternate artifact-binding evidence parent acceptance, missing physical operator-root identity commitment, and governed-store junction escape. The non-skipped Linux child body now builds and exercises the genuine public factory/dispatch/store/offline-verifier flow with relative validity. GREEN review fixes are authorized.
- Task 2 implementation review-fix GREEN: `78ab5e512a2d7f9299370c794eedd6ba23126d4f` (`fix(authority): close governed implementation gaps`), parent RED `b4e9140`; ten authorized source/test paths changed. A fresh takeover audit removed the context-less lifecycle fallback, keyed reserve-time trust/signing authority by exact capability, strengthened stable no-follow confined receipt IO, and verified semantic equal-race adoption/unequal fork refusal in addition to all review fixes. Clean-tree evidence: review suite 69/68/0/1; exact Task 2 suite 226/222/0/4; critical lifecycle/coordinator/factory 23/22/0/1; package/ABI 12/12; TypeScript/contracts/build/frozen/range/subjects/51-path gates pass; adapter digest unchanged; tracked status clean. Full report: `task-2-review-fix-report.md`. Same-reviewer scoped re-review is required; local Linux positive remains explicitly owed.
- Task 2 implementation scoped re-review 2: spec FAIL, quality/executability FAIL, `FIX FIRST`; full report `task-2-final-rereview.md`. Closed: accepted provenance, actual Job Card/route/deployment/head derivation in the main path, binding-parent/evidence join, capability-keyed reserve trust, physical operator-root digest, public type cleanup, semantic equal-node adoption, and real Linux child body. Remaining bounded blockers: production lifecycle publication refreshes only a frozen startup context while the test directly injects a new pin; governed store directory checks are not anchored across path-based writes and accept noncanonical identity strings; chosen route scope is not joined to selected definition endpoint membership, connector allowlists, and profile provider; and required 13-row admission, five-phase constructor, mutation, replacement, and CAS matrices remain non-load-bearing. No new path or architecture expansion required. Another strict tests-first bounded wave and scoped re-review are authorized.
- Task 2 final bounded RED: `a2d1dbf36896708f823a82ae258c4512071eac08` (`test(authority): expose final governed review gaps`), parent `78ab5e5`, changes seven authorized test/fixture paths; follow-up test-only CAS commit `c3431038252ec5bcaec0c7b564d7e1ef70331f5a` (`test(authority): cover governed publication races`) changes two of those paths. No production changed; tree clean and both commits pass `git show --check`. Full bounded suite: TypeScript pass; 80 total / 73 pass / 5 intentional failures / 2 exact skips. Failures reproduce production lifecycle not refreshing the Cell-owned revoked pin, profile-provider mismatch admitted, a 13-row pack substitution creating ledger/decision/receipt directories before refusal, noncanonical `..` reservation ID accepted, and real governed publication failing internal-integrity after source/route/identity before prepare/send. Real five-phase constructor and six-read loader matrices pass. Two independently imported publisher instances over one store pass equal adoption and refuse unequal fork, providing load-bearing CAS coverage. Production GREEN is authorized.
- Task 2 final bounded GREEN: `aee6dea5c53486614e5242874c4ff593c4c6959b` (`fix(authority): close final governed review gaps`), parent CAS RED `c343103`; twelve authorized source/test paths changed. Fixes production lifecycle per-publication Cell-owned pin refresh with explicit tenant/Cell/task, exact route/definition/connector/provider joins, 13-row no-side-effect admission ordering, canonical internal reservation IDs, physically anchored exclusive no-follow receipt writes with post-write root/parent/inode verification, accepted typed-array provenance handling, dynamic route generation/expiry propagation, and real public MCP publication/offline verification. Clean-tree evidence: focused 81/79/0/2; expanded 234/230/0/4; receipt/store/publication 11/11; TypeScript, both contracts, build, package/ABI, unchanged adapter digest, frozen/range/whitespace/14-subject/51-path checks all pass; tracked status clean. Full private report: `.superpowers/sdd/task-2-final-bounded-fix-report.md`. Fresh scoped review is required; Linux factory and privilege-dependent symlink evidence remain local skips, not passes.
- Task 2 final approval review: spec FAIL, quality/executability FAIL, `FIX FIRST`; full report `task-2-final-approval-review.md`. Closed: production lifecycle refresh, exact route/provider joins, canonical identifiers, public MCP publication/offline verification, real five-phase construction, equal/unequal semantic CAS, public/frozen boundaries. Remaining bounded blockers: store node creation is still pathname-based after parent identity capture, permitting swap/open/restore redirected writes; loader child opens are not handle-anchored to the pinned root inode; certified dispatch omits direct before/after/prepared generation equality to the gate-persisted route generation before prepare; and reservation/store counters, live replacement races, exhaustive outer closed-field mutations, and lifecycle expiry/head-advance public-path tests remain incomplete. No new path, ABI, ledger, or architecture expansion is required. Another strict tests-first bounded wave is authorized; Linux/privilege evidence remains honest debt.
- Task 2 physical-boundary RED: `5456264e5c15f2eed2f72ef7d34d654f452aab1b` (`test(authority): expose physical authority boundary gaps`), parent `aee6dea`; five authorized test paths, no production, clean tree. TypeScript passes; focused suite 90 total / 81 pass / 5 intentional failures / 4 exact skips. Failures prove the stale/colluding generation reaches credential, prepare, and commit before frozen ledger refusal, plus two deterministic loader and two governed-store swap/open/restore races requiring package-private physical-open barriers. Actual reservation/store filesystem observations replace fake counters; schema-driven exhaustive outer corpus and real lifecycle expiry/head-advance cases pass. Production GREEN is authorized.
- Task 2 physical-boundary GREEN: `2d6af3ac61762c61dc1658cf039d278a8c686efa` (`fix(authority): anchor governed authority filesystem boundaries`), parent RED `5456264`; authorized dispatch/loader/store source/test paths only. Adds direct persisted-generation joins before prepare and after prepare; retained Linux directory-handle child opens via `/proc/self/fd`; exclusive anchored store creation/read/cleanup; package-private non-barrel deterministic race barriers; root/child post-parse identity revalidation; and preserves identity-before-route latency chronology. Clean-tree evidence: focused 90/86/0/4; physical focus 32/30/0/2; expanded 243/237/0/6; stale/order 20/20; TypeScript, contracts, build, unchanged adapter digest, frozen/range/whitespace/51-path checks pass; tracked status clean. Report: `.superpowers/sdd/task2-physical-anchor-green-report.md`. Fresh approval review is required; Linux executor/privileged evidence skips remain honest.
- Task 2 physical final review: seven prior blocker groups closed; one Important durable-store semantic remains. `loadStoredChain` catches any `ENOENT` across prior existence/confinement/anchor capture and returns verified absence, so a root/reservation removal or swap race can become `null` rather than tamper/unreadable refusal. Exact bounded fix is only `src/authority/host/profile-governed-receipt.ts` plus its test: distinguish initial proven absent from disappearance after observation; close exceptional handle-stat paths. All fresh gates/scopes otherwise pass. A final two-file tests-first fix and scoped re-review are authorized.
- Task 2 disappearance RED: `019ec9b974c75822e581fab7c16abcc87f56eaf8` (`test(authority): distinguish governed absence from disappearance`), parent `2d6af3a`; one authorized test file only. Focused 17 registrations: 10 pass / 6 intentional fail / 1 Linux skip. True initial absence/readable empty controls return null; five root/governed/reservation remove-or-swap cases plus parent aggregation expose post-observation `ENOENT` collapsing. Tree clean.
- Task 2 disappearance GREEN: `334d5bb38248667ca689ad216c2a0953e9be232b` (`fix(authority): reject governed store disappearance`), parent RED `019ec9b`; one authorized production file only. Initial absence is proven by listing a retained parent; after child observation every confinement/barrier/anchor/open/read disappearance throws unreadable/tamper, and handle-stat exceptions close the fd. Clean-tree evidence: focused 17/16/0/1; exact boundary set 97/93/0/4; expanded Task2 250/244/0/6; TypeScript/contracts/build/frozen/51/range/subjects/status all pass. Report `.superpowers/sdd/task2-governed-disappearance-green-report.md`. Final scoped approval pending.
- Task 2 disappearance final review: spec compliance PASS; code quality/executability PASS; `APPROVED — SHIP Task 2`; no blockers. Fresh independent evidence: TypeScript pass; focused 17/16/0/1; physical boundary 97/93/0/4; expanded Task 2 250/244/0/6; package/ABI 12/12; Authority and Outcome Profile contract checks plus build pass; adapter digest unchanged; scoped 2/2 and cumulative 51/51 allowlists exact; frozen paths 0; 18 commit whitespace checks pass; tracked status clean. Linux remains an exact honest skip, not evidence. Review: `task-2-disappearance-final-review.md`.
- Task 2: complete at `334d5bb38248667ca689ad216c2a0953e9be232b`; all required RED/GREEN waves, bounded review fixes, independent spec/quality approval, and ledger closure are complete. Nonblocking debt retained for later verification: unavailable local Linux executor and a pre-existing out-of-range loader `handle.stat()` exceptional close gap.
- Task 2 root verification checkpoint: from clean HEAD `334d5bb`, fresh TypeScript test compilation passed; the focused governed-receipt suite passed 17/16/0/1 with the exact unavailable-Linux skip; Authority and Outcome Profile contract checks passed; build passed; tracked status remained clean. Task 3 may begin.
- Task 3: BASE `334d5bb38248667ca689ad216c2a0953e9be232b`; fresh implementer owns exactly the Task 3 `Files:` block (24 tracked paths), with strict RED subject `test(bootstrap): specify bootstrap contracts` and GREEN subject `feat(bootstrap): freeze project and runtime contracts`. No Task 3 production file may be created before the test-only RED commit and documented expected missing-module failure.
- Task 3 RED: `792b4cd40baba85e9fcdd436cad8bb0115bca258` (`test(bootstrap): specify bootstrap contracts`), parent exact BASE `334d5bb`; exactly the five authorized test paths changed and no production/schema/package paths existed. TypeScript failed only with five intended TS2307 missing-module diagnostics for `src/bootstrap`, `src/routes`, and `src/runtime`; tracked status clean.
- Task 3 GREEN candidate: `112b2ce70d3d5688d82be915d1169aeb8509c34a` (`feat(bootstrap): freeze project and runtime contracts`), parent RED `792b4cd`; BASE..HEAD contains exactly 24/24 authorized paths. Fresh implementer evidence: TypeScript pass; focused 22/22; Authority, Outcome Profile, and Bootstrap contract checks pass; build and diff check pass; public runtime exports 13/13; required packed paths 5/5; bootstrap digest `sha256:3b161491c1a8c0edb69e58b5d6c97713aa481778c30843127360e5188ebb20b6`; tracked status clean. Actual disposable tarball installation remains reserved to Task 7 by preflight ruling; Task 3 proves self-import and dry-run packed contents. Report `task-3-report.md`; fresh independent review pending.
- Task 3 review 1: spec compliance FAIL; code quality/executability FAIL; `FIX FIRST`. Mechanical gates, strict ancestry, exact 24-path scope, generated digest, exports, dry-run packaging, and Task 7 tarball-install reservation all pass. Four reproduced bounded blockers: runtime/route parsing accepts shell launch, credential-bearing/embedded endpoint argv, and URL evidence refs; proxy traps execute before inert refusal; installed-build membership diverges from npm shipped membership under nested `.npmignore`; and SupervisorStatus aggregate semantics reject valid uncovered-only rows while accepting impossible totals. Full findings `task-3-review.md`. A strict tests-first review-fix wave is authorized within existing Task 3 paths, followed by scoped re-review.
- Task 3 review-fix RED: `4efa08a537512b043c45c381c1f2b80dd91214c8` (`test(bootstrap): expose contract review gaps`), parent `112b2ce`; exactly four authorized test files changed, production untouched, tracked status clean. TypeScript compiled; focused runtime had 22 pass / 7 intentional failures covering npm nested-ignore membership, top-level/nested zero-trap proxy refusal, uncovered-only supervisor status, URL evidenceRef, shell executable/mode, and credential/embedded-endpoint argv.
- Task 3 review-fix GREEN: `d89c322adcc5ad19239afcddf1c5770bb6a8ba59` (`fix(bootstrap): close contract review gaps`), parent RED `4efa08a`; 14 changed paths, all within original 24. Fresh evidence: TypeScript pass; focused 29/29; three contract checks, build, diff check, and exact reviewer witnesses pass; bootstrap digest `sha256:8be3d4307c6cc265a3d1b6702b71f0ff4d09687a72e3813c5fb3df36abb9fb24`; installed digest `sha256:e650200e6b568044abbc109aeee89f280aa876aad9cd589ffe735f0990e277b0`; tracked status clean. Exact package membership now invokes the available npm CLI and fails closed if unavailable; no dependency/lockfile/new path/install/network change. Scoped same-reviewer re-review pending.
- Task 3 scoped re-review 1: three of four original findings closed; npm CLI membership is nonblocking under the pinned `npx` runtime because invocation is shell-free, offline, ignore-scripts, deterministic/non-mutating in witnesses, and fails closed. One bounded runtime-argv blocker remains: traversal/absolute private paths and credential channels such as `--config=../../outside`, `/etc/passwd`, `C:/Users/operator/private.pem`, `--header=Authorization:opaque-value`, and `--cookie=sessionid=opaque-value` are still accepted. Verdict remains `FIX FIRST`; another tests-first bounded wave within existing runtime test/manifest/schema paths is authorized.
- Task 3 argv RED: `693fb1668bfc593c8549e7e31c83a249183ed03b` (`test(bootstrap): expose runtime argv review gaps`), parent `d89c322`; only the authorized runtime test changed and exact path/credential-channel witnesses failed before production.
- Task 3 argv GREEN: `af8f99dcb35a1d3ead03c25231a7e268c1b6fb31` (`fix(bootstrap): confine runtime launcher arguments`), parent RED `693fb16`; six paths, all within the original Task 3 allowlist. Fresh evidence: TypeScript pass; focused 32/32 including all prior witnesses; three contract checks, build, diff check, runtime exports 13/13, packed paths 5/5 pass; bootstrap digest `sha256:e2cdd789b8dbc375e6cee7dd6109742780d6f26c1be73f980e640907d030a8ec`; installed digest `sha256:9dc69e3b014b1ebb0d00cdb45b00b30a2ec062aa5afe37fc4cbff815e1ad5df4`; status clean. Structural safe argv grammar is frozen here; Task 6 adapter validation owns exact adapter-specific option sets without changing this public interface. Final scoped re-review pending.
- Task 3 final scoped re-review: spec compliance PASS; code quality/executability PASS; `APPROVED — SHIP Task 3`; no blockers. Reviewer independently reproduced the test-only RED, exact six-path GREEN, all path/auth/header/cookie witnesses, focused 32/32, three contracts, build, digests, exports, package evidence, and clean status. npm CLI latency/availability and Task 6 exact adapter/topology validation remain nonblocking follow-up obligations. Review `task-3-review.md`.
- Task 3 root verification checkpoint: clean HEAD `af8f99d`; fresh TypeScript compilation, focused 32/32, Authority/Outcome Profile/Bootstrap contract checks, and build all passed; tracked status remained clean.
- Task 3: complete at `af8f99dcb35a1d3ead03c25231a7e268c1b6fb31`.
- Task 4: BASE `af8f99dcb35a1d3ead03c25231a7e268c1b6fb31`; fresh implementer owns exactly the Task 4 `Files:` block (10 tracked paths), with strict RED subject `test(bootstrap): specify route discovery` and GREEN subject `feat(bootstrap): report route-level coverage`. Existing initialization checkpoint IDs and report bytes are frozen; named-bootstrap route artifacts live only outside `.reelier/init/`.
- Task 4 RED: `dc35787e670212e5cc6a75ffd38d392b704502a9` (`test(bootstrap): specify route discovery`), parent exact BASE `af8f99d`; exactly five authorized test paths, production untouched, clean status. TypeScript failed only with five intended TS2307 diagnostics for the missing adapter/discovery/host modules. GREEN source work began only after RED and remains uncommitted within the five owned source paths.
- Task 4 SDD STOP CONDITION — accepted-plan/wire contradiction: Task 4's literal source-qualified route IDs use `:` (`host-config:gmail.send`, `plugin-manifest:gmail.send`, `direct-http:gmail.send`), while the independently approved Task 3 `RouteCoverageV1.routeId` schema is an opaque bounded ID with alphabet `^[A-Za-z0-9._~-]{1,128}$`, excluding `:`. Focused GREEN reached 71 total / 68 pass / 3 fail; two failures are exactly the schema conflict and one is an in-scope selector defect intentionally left unfixed after stopping. Read-only first-principles ruling recommends preserving the frozen Bootstrap contract and superseding only the presentation literal with canonical opaque source-separated IDs using `~` (for example `host-config~gmail.send`); `discoverySource` remains semantic authority and consumers must never parse the delimiter. This requires a docs-only Task 4 plan amendment plus a test-only correction commit before resuming current GREEN; no Task 3 schema/generated digest/ABI change. Explicit approval required before modifying the accepted plan.

- Task 4 final completion: `8c4bdf5` (`feat(bootstrap): report route-level coverage`) is independently approved — SHIP. Fresh TypeScript, focused 78/78, bootstrap contract, build, diff, Task 3 drift, exact allowlist, and clean status all pass. The implementation uses one adapter per harness, opaque route IDs, sealed snapshots, conservative evidence, and confined atomic route artifacts.
- Task 5 dispatch: exact BASE `8c4bdf5d1136c350114d3a0cd8e243e942423287`; task brief `.superpowers/sdd/2026-08-14-one-command-agent-bootstrap/task-5-brief.md`; fresh implementer `task5_named_init`. Strict RED subject `test(cli): specify named initialization`; wrong-checkout dispatch was interrupted and must resume only in `codex/reelier-init-up-implementation`.
- Task 5 contract ruling: the behavioral `actions`/`pathC` view is required in memory, but the Task 3 `BootstrapReportV1` wire schema is frozen and closed. Preserve the parser/schema and persist only the closed report; return a typed structural named-preparation extension from `initializeAgentProject`, with no secret-bearing or persisted extension fields. Cost if wrong: local API shape differs from the literal pseudocode, but wire compatibility and Task 3 digest remain intact.
- Task 5 RED: `eecfab61f835489ac9aae1351ce555292b93499b` (`test(cli): specify named initialization`), parent exact Task 4 GREEN `8c4bdf5`; only Task 5 allowlisted tests changed. TypeScript failed only with intended missing `src/bootstrap/initialize.js` and `src/bootstrap/install.js` APIs; production remains untouched. GREEN is authorized.
- Task 5 review: `FIX FIRST`, report `task-5-review.md`. The reviewer reproduced missing durable transaction/revalidation, pre-refusal filesystem confinement, invalid/fabricated artifact joins, placeholder install/governance/canary phases, constant-only maker/checker proof, absent adversarial matrix, and unsafe workload-key restart/concurrency semantics. A tests-first bounded review-fix wave is authorized within the existing Task5 allowlist; Task6 is blocked until scoped re-review approves.
- Task 5 review-fix 1: RED `b775ba1`, GREEN `535cd98`, report update `0d4b5c0`; 52 focused tests pass, but independent re-review `task5_review_fix` remains `FIX FIRST`. Blocking findings: no real bootstrap lock/stale recovery, checkpoint IDs without artifact digests, unsafe concurrent/project-unscoped workload keys, unreachable install/apply/consent/backup/rollback/canary, and unsupported valid governance import. Review-fix round 2 is authorized; Task6 remains blocked.
- Task 5 review-fix 2: RED `5ce53dc`, GREEN `458504d`, report `a1ba08e`; 57 focused tests pass, but independent re-review found live PID stale-lock refusal, tampered checkpoint/path acceptance, cross-project key reuse, post-install rollback/read-back gaps, and fabricated governance acceptance. Review-fix round 3 is authorized.
- Task 5 review-fix 3: RED `fb4121d`, GREEN `6d3ebee`, report `4536602`; 61 focused tests pass, but fresh integrity review still finds self-asserted governance, synthetic installed/Continuity digests, incomplete checkpoint joins, and rollback/canary gaps. Review-fix round 4 governance ruling: use existing Task2 `loadProfileGovernanceFromOperatorTrust` with the six-file fixed-root fixture; reject home summary files; optional internal governance reference only, no arbitrary CLI trust fields.
- Task 5 review-fix 4: RED `b8bbaa7` exposed synthetic installed-build provenance and self-authored governance acceptance. GREEN `e6a3adc` switched to canonical installed-build computation, rejected the summary, added the fixed-root Task2 governance reference, and pinned the homedir identity. Test fixture migration `e1bc87f` uses the signed six-file Task2 governance fixture. Fresh named-init file gate: 22/22; TypeScript/contracts/build/diff/status clean. Independent fresh review still reports FIX FIRST on exact checkpoint joins/resume, case-colliding names, project-root junction confinement, install rollback/read-back/legacy preservation, governance tenant join, and remaining transaction witnesses. Task6 remains blocked; next RED/GREEN wave is authorized within Task5 allowlist.
- Task 5 reset ruling (approved 2026-08-15): repeated bounded fixes through current HEAD
  `e6faee9` did not produce approval for the broad named-initialization transaction. Freeze the
  smallest outcome instead: one physically confined, exact-version/build-pinned, observation-ready
  local project generation plus honest report and exact persisted `npx reelier@<exact-version> up`
  recovery command. Existing Task 5 commits remain history and evidence only; they do not authorize
  retaining behavior outside the reset contract.
- Task 5 reset scope: essential now is canonical name/root validation, build pinning, the closed
  artifact generation, and explicit lock/checkpoint/publication/rollback/restart behavior. Deferred
  to separately governed amendments: governance/Authority, broad MCP/plugin installation or
  interception, workload keys/registration, provider/network canaries, managed-Cell connection,
  and runtime/fleet/subagent supervision. No placeholder or optimistic claim is permitted.
- Task 5 reset execution is three sequential review units: 5A contract-only RED
  (`test(cli): reset named initialization contract`); 5B minimal transaction GREEN
  (`feat(cli): prepare minimal named project`); 5C proof-only E2E
  (`test(cli): prove minimal named initialization`). The state machine is closed as
  `absent -> locked -> prepared -> committing -> complete`, with failures entering
  `rolling-back` and any unproved rollback entering durable `recovery-required`. Restart must
  finish recorded rollback before any forward work and may never adopt drifted bytes.
- Task 6 remains BLOCKED. Promotion requires exactly three load-bearing real CLI/filesystem E2E
  scenarios from a clean tree: clean first initialization; forced crash/restart at every durable
  transition; and publication/final-reread failure with byte-exact rollback and successful retry,
  including a concurrent-initializer zero-write refusal. Supporting unit/schema/type checks do not
  satisfy this gate. Exact outputs and artifact digests plus independent 5A–5C approval must be
  recorded before Task 6 dispatch.
