# Claude Code observed capture — 2026-08-16

This is a genuine observation from a running Claude Code CLI instance on Windows. It inventoried
the instance's MCP routes and attempted the governed `reelier authority serve` boundary. The
boundary refused with the Linux-only Authority Cell requirement before the semantic delegation
operations became callable.

The attached capture is therefore `observed` and non-passing. It proves the refusal and the
observed route inventory only; it does not prove live semantic execution, enforcement, or
completeness. Claude Code remains `not-tested` in the aggregate until a Linux Authority Cell is
reachable from the actual harness instance.
