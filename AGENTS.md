# Reelier execution paths (audited 2026-08-01, code pin `87eab04c25a0b9e01aafe95740d0f6825fe6da00`)

Pinned capability summary: Path A (`reelier mcp --wrap`) records live MCP traffic and applies its policy seatbelt; malformed policy fails open with a warning/attestation claim. Path B (`reelier run`) replays a frozen skill and fails closed on manifest/state-gate drift. Path C currently ships only its v1 closed wire schemas, JCS digest/signature primitives, deterministic vectors, and public export. The signed v1 wire now carries sponsor/audience, delegation root-or-child constraints, connector/account, source projection authority, risk/limits, canonical policy commitment, and path-bound source claims. Chain attenuation, trust, activation/revocation, source/contract matching, gate, ledger, ingress, credential broker, HTTPS driver, and packs remain designed/unbuilt. Path C's intended promise is to delegate outcomes, not credentials, and to prove bounded scope rather than safety or content correctness.
