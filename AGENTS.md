# Reelier execution paths (audited 2026-08-02, code pin `be8931b9acdad0d5177e2ca61d4ed1175bd01fe9`)

Pinned capability summary: Path A (`reelier mcp --wrap`) records live MCP traffic and applies its policy seatbelt; malformed policy fails open with a warning/attestation claim. Path B (`reelier run`) replays a frozen skill and fails closed on manifest/state-gate drift. Path C currently ships its v1 closed wire schemas, JCS digest/signature primitives, deterministic vectors, pure standing-authority validation/compilation, crash-safe local atomic authority ledger, and public export. The signed v1 wire carries sponsor/audience, delegation root-or-child constraints, connector/account, source projection authority, risk/limits, canonical policy commitment, and path-bound source claims. GateEvent and AuthorityReceipt bind an exact closed DecisionContext covering tenant, requester, request, contract, capability, outcome, effect, and source/authority snapshots; the public wire layer validates the embedded receipt preimage and portable receipt-to-GateEvent digest chain. The filesystem ledger strictly binds canonical request/capability preimages, ingress/outcome/capability/limit claims, a monotonic clock, and the durable reservation/dispatch/result state machine; POSIX directory sync is verified while Windows directory sync is reported best-effort. Gate/evidence runtime, ingress, credential broker, HTTPS driver, and packs remain designed/unbuilt. Path C's intended promise is to delegate outcomes, not credentials, and to prove bounded scope rather than safety or content correctness.
