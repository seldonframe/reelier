# Reelier execution paths (audited 2026-08-02, code pin `f8f0e31ae0fbd9675f4655949d4026b0c78ae367`)

Pinned capability summary: Path A (`reelier mcp --wrap`) records live MCP traffic and applies its policy seatbelt; malformed policy fails open with a warning/attestation claim. Path B (`reelier run`) replays a frozen skill and fails closed on manifest/state-gate drift. Path C currently ships its v1 closed wire schemas, JCS digest/signature primitives, deterministic vectors, pure two-phase standing-authority validation/compilation, deterministic kernel-owned multi-read source materialization, crash-safe local atomic authority ledger, and public export. The signed v1 wire carries sponsor/audience, delegation root-or-child constraints, connector/account, source projection/read/freshness authority, risk/limits, canonical policy commitment, and exact path-bound source claims. CompiledCapability seals authenticated tenant/requester/definition alias, request, contract, source and authority snapshots, exact quantitative limits, outcome/effect, and time; DecisionContext and AuthorityReceipt carry the authenticated alias and bind the portable digest chain. The v2 filesystem ledger strictly binds those canonical request/capability preimages, exactly typed contract-window/source-trigger limits, ingress/outcome/capability claims, a monotonic clock, and the durable reservation/dispatch/result state machine; old v1 transaction records fail closed, POSIX directory sync is verified, and Windows directory sync is reported best-effort. Gate/evidence runtime, provider reads/writes, ingress, credential broker, HTTPS driver, and concrete packs remain designed/unbuilt. Path C's intended promise is to delegate outcomes, not credentials, and to prove bounded scope rather than safety or content correctness.
